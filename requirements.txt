# Strategy Agent — Python dependencies
# Full list per opus_WORKFLOW.md §6. Grouped by the phase that first needs each lib.
# Phase 1 installs only the "Core" + "Testing" + "Code Quality" groups (see README).
# Heavy Phase 2-5 libs are installed in their phase to avoid Windows build issues.

# --- Core (Phase 1) ---
pydantic>=2.0.0
pyyaml>=6.0
typer>=0.9.0
rich>=13.0.0
questionary>=2.0.0               # arrow-key /model and /role pickers in the REPL (core/picker.py)
python-dotenv>=1.0.0

# --- LLM client (Phase 1) — OpenAI-compatible local endpoints ---
openai>=1.0.0
requests>=2.31.0
httpx>=0.25.0

# --- Research (Phase 2+) ---
aiohttp>=3.9.0
trafilatura>=1.6.0
diskcache>=5.6.0

# --- Document reading (Phase 2+) ---
pdfplumber>=0.10.0               # Text-based PDFs
pytesseract>=0.3.10              # OCR for scanned PDFs
Pillow>=10.0.0                   # Image handling
python-docx>=1.1.0               # Word document reading (.docx reader: core/docx_reader.py)

# --- Dimensions: Analyst (Recruiting) + Builder (Finance), Phase 6+ ---
# High-fidelity local document reading (tables / multi-column / OCR). MIT-licensed, fully offline.
# OPTIONAL: the .docx/.pptx/.pdf/.xlsx readers work without these; the Docling adapter
# (core/docling_reader.py) fails open and Porter falls back to the lightweight readers if absent.
docling>=2.0.0                   # IBM, MIT — preferred high-fidelity reader (PDF/DOCX/PPTX/XLSX/img)
# markitdown>=0.0.1              # Microsoft, MIT — fast Office→Markdown fallback (optional)
# (python-pptx, listed under Output below, doubles as the .pptx reader: core/pptx_reader.py)

# --- Output (Phase 4+) ---
python-pptx>=0.6.23
matplotlib>=3.8.0                # Themed image-charts (labeled axes) drawn into decks/briefs
weasyprint>=60.0
jinja2>=3.1.0
openpyxl>=3.1.0                  # Create/write/format .xlsx files
pandas>=2.0.0                    # Read user-provided .xlsx files
xlsxwriter>=3.1.0               # Advanced chart/format fallback for Excel

# --- Memory (Phase 5+) ---
chromadb>=0.5.0

# --- Voice (Phase 5+) ---
faster-whisper>=0.10.0
pyaudio>=0.2.14
pynput>=1.7.0                    # Keyboard hotkey detection

# --- Testing ---
pytest>=7.4.0
pytest-asyncio>=0.21.0

# --- Code quality ---
ruff>=0.4.0
mypy>=1.8.0
