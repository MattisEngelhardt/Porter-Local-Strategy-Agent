{# T-3 Market Overview Brief (SPEC §10). Market at a glance + trends + Neura's position. #}
{% import "_macros.md.j2" as m %}
<!doctype html><html><head><meta charset="utf-8">{% include "_styles.md.j2" %}</head>
<body>
{% include "_head.md.j2" %}
{{ m.bottomline(bl_label, bottom_line) }}
{{ m.artifactframe(artifact_label, artifact_type, artifact_audience, evidence_label, evidence_anchors, gap_note, artifact_focus_label, artifact_focus, proof_stat_label, proof_stat, source_stat_label, source_stat, gap_stat_label, gap_stat) }}
{{ m.sections(sections) }}
{{ m.sources(sources_label, sources) }}
{% if note %}<div class="note">{{ note }}</div>{% endif %}
</body></html>
